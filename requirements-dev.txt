# Dev/test dependencies — installed locally, not deployed to Lambda
boto3>=1.34.0
pytest>=8.0.0
pytest-cov>=5.0.0
moto[dynamodb,s3,sns,events,iam,lambda,config,securityhub]>=5.0.0
bandit>=1.7.8
ruff>=0.4.0
